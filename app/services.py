def send_notification(user_id, message):
    """
    Envoie une notification à l'utilisateur spécifié.
    Retourne True si la notification a été envoyée avec succès, False sinon.
    """
    # Logique pour envoyer la notification, par exemple via SMTP ou une API de notifications
    print(f"Sending notification to user {user_id}: {message}")
    return True